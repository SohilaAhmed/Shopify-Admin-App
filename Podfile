# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'Shopify Admin App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Shopify Admin App

pod 'Kingfisher'

  target 'Shopify Admin AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Shopify Admin AppUITests' do
    # Pods for testing
  end

end
